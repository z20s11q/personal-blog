---
title: "无畏并发"
order: "ch16-00-concurrency"
chapter: 16
---
并发：不同部分独立执行。并行：同时执行。本章说“并发”时，包含二者。

所有权和类型检查把许多并发错误挡在编译期，而不是留到运行时。

本章四块：

- 用线程同时跑多段代码
- 消息传递：channel 在线程之间发数据
- 共享状态：多线程访问同一份数据
- `Send` 与 `Sync`：把这些保证扩展到自定义类型

标准库同时提供消息传递和共享内存，按场景选。

---
title: "模式与匹配"
order: "ch19-00-patterns"
chapter: 19
---
模式按类型的结构去匹配值，用来决定控制流。组成可以是：

- 字面量
- 解构后的数组、enum、struct、元组
- 变量
- 通配符
- 占位符

例如 `x`、`(a, 3)`、`Some(Color::Red)`。与某个值比较：形状相符就使用绑到的那几部分；不相符，对应代码不执行。

本章是模式的参考：能出现的位置、可反驳与不可反驳、模式语法。
